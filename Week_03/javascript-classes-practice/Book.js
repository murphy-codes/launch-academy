class Book {
  constructor(title, pageLength, publicationYear) {
    this.title = title;
    this.pageLength = pageLength;
    this.publicationYear = publicationYear;
  };
};

export default Book;